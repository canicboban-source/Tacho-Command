import {readFileSync,writeFileSync} from "node:fs";
const edits=[{"path":"lib/app-v2-card-transport-controller-bridge.js","before":"import { readBrowserAppV2GoldenCardPayload } from \"./app-v2-golden-card-browser-transport.js\";","after":"import { readAppV2GoldenCardPayload, readBrowserAppV2GoldenCardPayload } from \"./app-v2-golden-card-browser-transport.js\";\nimport { createDeferredCardDeviceChooser } from \"./app-v2-card-ready-gate.js\";"},{"path":"lib/app-v2-card-transport-controller-bridge.js","before":"  onProgress,\n} = {}) {","after":"  onProgress,\n  onDeviceSelected,\n  onTransportError,\n} = {}) {"},{"path":"lib/app-v2-card-transport-controller-bridge.js","before":"    readCompletedPayload: async () => {\n      const transportResult = await readBrowserAppV2GoldenCardPayload({\n        ...transportOptions,\n        onProgress,\n      });\n      return transportResult.payload;\n    },","after":"    readCompletedPayload: async () => {\n      try {\n        const options = { ...transportOptions, onProgress };\n        // The original public app uses this unmodified direct path.\n        // Only the experimental V18 field gate wraps the device chooser.\n        const transportResult = onDeviceSelected\n          ? await readAppV2GoldenCardPayload({\n              ...options,\n              bluetooth: createDeferredCardDeviceChooser({\n                bluetooth: typeof navigator === \"undefined\" ? null : navigator.bluetooth,\n                onDeviceSelected,\n              }),\n            })\n          : await readBrowserAppV2GoldenCardPayload(options);\n        return transportResult.payload;\n      } catch (error) {\n        try { onTransportError?.(error); } catch {}\n        throw error;\n      }\n    },"},{"path":"app/app-v2/app-v2-client.tsx","before":"import { useEffect, useMemo, useState } from \"react\";","after":"import { useEffect, useMemo, useRef, useState } from \"react\";"},{"path":"app/app-v2/app-v2-client.tsx","before":"  const [phoneZoneKey, setPhoneZoneKey] = useState<string | null>(null);","after":"  const [phoneZoneKey, setPhoneZoneKey] = useState<string | null>(null);\n  const [awaitingCardRecognition, setAwaitingCardRecognition] = useState(false);\n  const [cardReadyError, setCardReadyError] = useState<string | null>(null);\n  const cardReadyConfirmRef = useRef<(() => void) | null>(null);"},{"path":"app/app-v2/app-v2-client.tsx","before":"    const readingSession = beginAppV2CardRead(cardSession);\n    setCardSession(readingSession);\n    setCardReadProgress(Object.freeze({ submessages: 0, byteLength: 0, complete: false }));\n\n    const result = await runBrowserAppV2GoldenCardRead({","after":"    const readingSession = beginAppV2CardRead(cardSession);\n    setCardSession(readingSession);\n    setCardReadyError(null);\n    setAwaitingCardRecognition(false);\n    setCardReadProgress(Object.freeze({ submessages: 0, byteLength: 0, complete: false }));\n\n    const result = await runBrowserAppV2GoldenCardRead({"},{"path":"app/app-v2/app-v2-client.tsx","before":"      onProgress: (progress: CardReadProgress) => setCardReadProgress(progress),\n    });\n\n    if (result.session) setCardSession(result.session);","after":"      onDeviceSelected: () => new Promise<void>((resolve, reject) => {\n        // The golden reader has selected the physical device but has not\n        // connected GATT or sent ANY command. No fixed delay is assumed to\n        // establish tachograph card readiness.\n        let finished = false;\n        const timer = window.setTimeout(() => {\n          if (finished) return;\n          finished = true;\n          cardReadyConfirmRef.current = null;\n          setAwaitingCardRecognition(false);\n          reject(new Error(\"Kartica nije potvrđena na tahografu u roku od 3 min.\"));\n        }, 180_000);\n        cardReadyConfirmRef.current = () => {\n          if (finished) return;\n          finished = true;\n          window.clearTimeout(timer);\n          cardReadyConfirmRef.current = null;\n          setAwaitingCardRecognition(false);\n          resolve();\n        };\n        setAwaitingCardRecognition(true);\n      }),\n      onProgress: (progress: CardReadProgress) => setCardReadProgress(progress),\n      onTransportError: (error: unknown) => {\n        setCardReadyError(error instanceof Error ? error.message.slice(0,180) : String(error).slice(0,180));\n      },\n    }).finally(() => {\n      cardReadyConfirmRef.current = null;\n      setAwaitingCardRecognition(false);\n    });\n\n    if (result.session) setCardSession(result.session);"},{"path":"app/app-v2/app-v2-client.tsx","before":"            versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V17\",","after":"            awaitingCardRecognition,\n            cardReadyError,\n            onConfirmCardReady: () => cardReadyConfirmRef.current?.(),\n            versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V18\","},{"path":"app/app/field-proven-premium-ui.tsx","before":"  versionLine: string;\n  phoneTimeLabel: string | null;","after":"  versionLine: string;\n  phoneTimeLabel: string | null;\n  awaitingCardRecognition: boolean;\n  cardReadyError: string | null;\n  onConfirmCardReady: () => void;"},{"path":"app/app/field-proven-premium-ui.tsx","before":"            {controls.phase === \"card-reading\" && \"Sačekajte završetak očitavanja kartice\"}","after":"            {controls.phase === \"card-reading\" && (controls.awaitingCardRecognition ? \"Sačekajte završetak prepoznavanja kartice\" : \"Sačekajte završetak očitavanja kartice\")}"},{"path":"app/app/field-proven-premium-ui.tsx","before":"            {controls.phase === \"card-reading\"\n              ? \"Očitavanje kartice je u toku…\"\n              : cardProgress?.complete","after":"            {controls.awaitingCardRecognition\n              ? \"Čekamo da tahograf prepozna karticu…\"\n              : controls.phase === \"card-reading\"\n                ? \"Očitavanje kartice je u toku…\"\n                : cardProgress?.complete"},{"path":"app/app/field-proven-premium-ui.tsx","before":"          {cardProgress ? (\n            <div className={styles.cardTransferProgress} aria-live=\"polite\">","after":"          {controls.awaitingCardRecognition\n            ? <small>Chrome je izabrao tahograf. Sačekajte da na displeju tahografa bude završen postupak prepoznavanja kartice; tek tada pritisnite dugme ispod. Dok čekate, ne šaljemo zahtev za čitanje.</small>\n            : null}\n          {controls.cardReadyError ? <small role=\"status\">{controls.cardReadyError}</small> : null}\n          {cardProgress && !controls.awaitingCardRecognition ? (\n            <div className={styles.cardTransferProgress} aria-live=\"polite\">"},{"path":"app/app/field-proven-premium-ui.tsx","before":"          onClick={controls.onReadCard}\n          disabled={controls.phase === \"connecting\" || controls.phase === \"card-reading\"}\n        >\n          {controls.phase === \"card-reading\" ? \"Očitavam…\" : \"Očitaj karticu\"}","after":"          onClick={controls.awaitingCardRecognition ? controls.onConfirmCardReady : controls.onReadCard}\n          disabled={controls.phase === \"connecting\" || (controls.phase === \"card-reading\" && !controls.awaitingCardRecognition)}\n        >\n          {controls.awaitingCardRecognition\n            ? \"Kartica prepoznata — počni čitanje\"\n            : controls.phase === \"card-reading\" ? \"Očitavam…\" : \"Očitaj karticu\"}"}];
for(const {path,before,after} of edits){
 const source=readFileSync(path,"utf8");const n=source.split(before).length-1;
 if(n!==1)throw Error("V18 source anchor "+path+" matches "+n+": "+before.slice(0,105));
 writeFileSync(path,source.replace(before,after));
}
for(const path of ["tests/v17-production-clock.test.mjs"]){
 const content=readFileSync(path,"utf8");
 if(!content.includes("PREVIEW V17"))throw Error("V18 test anchor absent");
 writeFileSync(path,content.replace(/PREVIEW V17/g,"PREVIEW V18")
 .replace(/assert\.doesNotMatch\(client,\/prepareAppV2CardHandoff\|transportOptions:\|keepGattConnected\/\);/, 'assert.doesNotMatch(client,/prepareAppV2CardHandoff|keepGattConnected/);'));
}
console.log("V18 chooser readiness gate installed; original golden file never changed.");
