import {readFileSync,writeFileSync} from "node:fs";
const edits=[{"path":"app/app-v2/app-v2-client.tsx","before":"import { prepareAppV2CardHandoff } from \"../../lib/app-v2-card-handoff.js\";","after":"import { prepareAppV2CardHandoff } from \"../../lib/app-v2-card-handoff.js\";\nimport { currentDeviceUtcOffsetMinutes, formatDeviceUtcOffset, projectCardStateToDeviceClock } from \"../../lib/app-v2-device-clock.js\";"},{"path":"app/app-v2/app-v2-client.tsx","before":"  const [cardDiagnostic, setCardDiagnostic] = useState<string | null>(null);","after":"  const [cardDiagnostic, setCardDiagnostic] = useState<string | null>(null);\n  const [deviceUtcOffsetMinutes, setDeviceUtcOffsetMinutes] = useState<number | null>(null);"},{"path":"app/app-v2/app-v2-client.tsx","before":"  }, []);\n\n  const state = useMemo(() => {","after":"  }, []);\n\n  useEffect(() => {\n    const syncPhoneClock = () => {\n      const next = currentDeviceUtcOffsetMinutes();\n      setDeviceUtcOffsetMinutes((previous) => previous === next ? previous : next);\n    };\n    const onVisibility = () => {\n      if (document.visibilityState === \"visible\") syncPhoneClock();\n    };\n    syncPhoneClock();\n    window.addEventListener(\"focus\", syncPhoneClock);\n    document.addEventListener(\"visibilitychange\", onVisibility);\n    const timer = window.setInterval(syncPhoneClock, 30000);\n    return () => {\n      window.removeEventListener(\"focus\", syncPhoneClock);\n      document.removeEventListener(\"visibilitychange\", onVisibility);\n      window.clearInterval(timer);\n    };\n  }, []);\n\n  const displayCardState = useMemo(() => {\n    const canonical = cardState ?? {};\n    return deviceUtcOffsetMinutes === null\n      ? canonical\n      : projectCardStateToDeviceClock(canonical, deviceUtcOffsetMinutes);\n  }, [cardState, deviceUtcOffsetMinutes]);\n\n  const state = useMemo(() => {"},{"path":"app/app-v2/app-v2-client.tsx","before":"      card: cardState ?? {},\n      localeLabel: \"SR · Srpski\",","after":"      card: displayCardState,\n      localeLabel: \"SR · Telefon \" + formatDeviceUtcOffset(deviceUtcOffsetMinutes ?? 0),"},{"path":"app/app-v2/app-v2-client.tsx","before":"  }, [cardState, lastLiveSnapshot, liveConnected, liveRunState, liveSession]);","after":"  }, [displayCardState, deviceUtcOffsetMinutes, lastLiveSnapshot, liveConnected, liveRunState, liveSession]);"},{"path":"app/app-v2/app-v2-client.tsx","before":"versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V14\",","after":"versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V15\","},{"path":"app/landing-page.tsx","before":"    guide: \"Vodič za povezivanje\",","after":"    guide: \"Vodič za povezivanje\",\n    safetyNote: \"Bezbednost pre svega: povezivanje, LIVE podatke i očitavanje kartice koristite samo dok vozilo miruje i bezbedno je zaustavljeno.\","},{"path":"app/landing-page.tsx","before":"open: \"Open app\", guide: \"Connection guide\",","after":"open: \"Open app\", guide: \"Connection guide\", safetyNote: \"Safety first: connect, view LIVE data and read the driver card only while the vehicle is stationary and safely parked.\","},{"path":"app/landing-page.tsx","before":"open: \"App öffnen\", guide: \"Verbindungsanleitung\",","after":"open: \"App öffnen\", guide: \"Verbindungsanleitung\", safetyNote: \"Sicherheit zuerst: Verbindung, LIVE-Daten und Fahrerkarten-Auslesen nur bei stehendem und sicher abgestelltem Fahrzeug verwenden.\","},{"path":"app/landing-page.tsx","before":"          <p>{t.heroText}</p>\n          <div className=\"tcx-actions\">","after":"          <p>{t.heroText}</p>\n          <div className=\"tcx-safety-note\"><strong>✓</strong><span>{t.safetyNote}</span></div>\n          <div className=\"tcx-actions\">"},{"path":"app/landing-oled.css","before":".tcx-hero-copy > p { max-width: 650px; margin: 0; color: #9aa3af; font-size: clamp(.98rem, 1.6vw, 1.12rem); line-height: 1.65; }\n.tcx-actions","after":".tcx-hero-copy > p { max-width: 650px; margin: 0; color: #9aa3af; font-size: clamp(.98rem, 1.6vw, 1.12rem); line-height: 1.65; }\n.tcx-safety-note {\n  max-width: 650px;\n  display: flex;\n  gap: 10px;\n  align-items: flex-start;\n  margin-top: 18px;\n  padding: 12px 14px;\n  border: 1px solid rgba(75, 220, 163, .24);\n  border-radius: 12px;\n  background: rgba(75, 220, 163, .055);\n  color: #aab5b0;\n  font-size: .76rem;\n  line-height: 1.55;\n}\n.tcx-safety-note strong { color: var(--tcx-green); }\n.tcx-actions"}];
for(const {path,before,after} of edits){
  const content=readFileSync(path,"utf8");
  const count=content.split(before).length-1;
  if(count!==1)throw new Error("V15 patch anchor "+path+" count="+count+" anchor="+before.slice(0,110));
  writeFileSync(path,content.replace(before,after));
}
for (const testPath of [
  "tests/v12-card-diagnostics.test.mjs",
  "tests/v13-diagnostics-handoff.test.mjs",
  "tests/v14-first-card-handoff.test.mjs",
]) {
  const source = readFileSync(testPath, "utf8");
  if (!source.includes("PREVIEW V14")) throw new Error("V15 prior preview marker missing in " + testPath);
  writeFileSync(testPath, source.replace(/PREVIEW V14/g, "PREVIEW V15"));
}
console.log("V15 phone-clock projection and localized landing safety notice applied");
