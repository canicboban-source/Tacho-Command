import { readFileSync, writeFileSync } from "node:fs";
const edits=[{"path":"app/app-v2/app-v2-client.tsx","before":"import { openBrowserAppV2FieldTransport } from \"../../lib/app-v2-field-transport.js\";","after":"import { openBrowserAppV2FieldTransport } from \"../../lib/app-v2-field-transport.js\";\nimport { getPhoneTimeZone, projectCardHistoryToPhoneZone } from \"../../lib/app-v2-phone-time-history.js\";"},{"path":"app/app-v2/app-v2-client.tsx","before":"  const [capturedAtIso, setCapturedAtIso] = useState<string | null>(null);","after":"  const [capturedAtIso, setCapturedAtIso] = useState<string | null>(null);\n  const [phoneTimeZone, setPhoneTimeZone] = useState<string | null>(null);\n  const [phoneClockSignature, setPhoneClockSignature] = useState(\"\");"},{"path":"app/app-v2/app-v2-client.tsx","before":"  const state = useMemo(() => {","after":"  useEffect(() => {\n    // There is no dependable browser time-zone-change event. Recheck the\n    // phone's IANA zone and current offset on focus, page resume and a short\n    // timer. Never overwrite the saved UTC card snapshot.\n    const refreshPhoneClock = () => {\n      const zone = getPhoneTimeZone();\n      const offset = new Date().getTimezoneOffset();\n      setPhoneTimeZone(zone);\n      setPhoneClockSignature(zone + \":\" + String(offset));\n    };\n    refreshPhoneClock();\n    const timer = window.setInterval(refreshPhoneClock, 30000);\n    window.addEventListener(\"focus\", refreshPhoneClock);\n    window.addEventListener(\"pageshow\", refreshPhoneClock);\n    document.addEventListener(\"visibilitychange\", refreshPhoneClock);\n    return () => {\n      window.clearInterval(timer);\n      window.removeEventListener(\"focus\", refreshPhoneClock);\n      window.removeEventListener(\"pageshow\", refreshPhoneClock);\n      document.removeEventListener(\"visibilitychange\", refreshPhoneClock);\n    };\n  }, []);\n\n  const state = useMemo(() => {"},{"path":"app/app-v2/app-v2-client.tsx","before":"      card: cardState ?? {},","after":"      // Reproject original UTC card activity on each phone zone change.\n      // This is for local display only; GOLDEN, localStorage and LIVE counters\n      // remain unchanged. Clip the last UTC day to the actual read instant.\n      card: cardState && phoneTimeZone\n        ? projectCardHistoryToPhoneZone(cardState, {\n            timeZone: phoneTimeZone,\n            capturedAtIso,\n          })\n        : cardState ?? {},"},{"path":"app/app-v2/app-v2-client.tsx","before":"  }, [cardState, lastLiveSnapshot, liveConnected, liveRunState, liveSession]);","after":"  }, [cardState, lastLiveSnapshot, liveConnected, liveRunState, liveSession, phoneTimeZone, phoneClockSignature, capturedAtIso]);"},{"path":"app/app-v2/app-v2-client.tsx","before":"            cardReadProgress,\n            cardDiagnostic,","after":"            cardReadProgress,\n            cardDiagnostic,\n            phoneTimeZoneLabel: phoneTimeZone === null\n              ? \"Učitavam vremensku zonu telefona…\"\n              : \"Vreme telefona · \" + phoneTimeZone\n                + \" (UTC\" + (new Date().getTimezoneOffset() <= 0 ? \"+\" : \"-\")\n                + String(Math.floor(Math.abs(new Date().getTimezoneOffset()) / 60)).padStart(2, \"0\")\n                + \":\" + String(Math.abs(new Date().getTimezoneOffset()) % 60).padStart(2, \"0\") + \")\"\n                + \" · tahograf čuva UTC\","},{"path":"app/app-v2/app-v2-client.tsx","before":"versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V14\",","after":"versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V15\","},{"path":"app/app/field-proven-premium-ui.tsx","before":"  cardDiagnostic: string | null;","after":"  cardDiagnostic: string | null;\n  phoneTimeZoneLabel: string;"},{"path":"app/app/field-proven-premium-ui.tsx","before":"function HistoryDayDetail({\n  day,\n  onBack,\n}: Readonly<{ day: FieldProvenHistoryDay; onBack: () => void }>) {","after":"function HistoryDayDetail({\n  day,\n  onBack,\n  phoneTimeZoneLabel,\n}: Readonly<{ day: FieldProvenHistoryDay; onBack: () => void; phoneTimeZoneLabel: string }>) {"},{"path":"app/app/field-proven-premium-ui.tsx","before":"        <h1>{day.dateLabel}</h1>\n        <p>24-časovni zapis aktivnosti sa tahografske kartice. Velike crte su sati, srednje 30 min, male 15 min.</p>","after":"        <h1>{day.dateLabel}</h1>\n        <p>{phoneTimeZoneLabel}</p>\n        <p>Prikaz aktivnosti prema vremenu telefona; velika podela je sat, srednja 30 min, mala 15 min. Tokom promene letnjeg/zimskog vremena 24h osa ne mora biti jednoznačna.</p>"},{"path":"app/app/field-proven-premium-ui.tsx","before":"function HistoryScreen({ state }: Readonly<{ state: FieldProvenProductState }>) {\n  const visibleDays = state.historyDays.slice(0, 56);","after":"function HistoryScreen({ state, controls }: Readonly<{ state: FieldProvenProductState; controls: ProductControls }>) {\n  const visibleDays = [...state.historyDays].sort((a, b) => b.dateLabel.localeCompare(a.dateLabel)).slice(0, 56);"},{"path":"app/app/field-proven-premium-ui.tsx","before":"  const [selectedDayIndex, setSelectedDayIndex] = useState<number | null>(null);\n  const selectedDay = selectedDayIndex === null ? null : visibleDays[selectedDayIndex] ?? null;","after":"  const [selectedDayDate, setSelectedDayDate] = useState<string | null>(null);\n  const selectedDay = selectedDayDate === null ? null : visibleDays.find((day) => day.dateLabel === selectedDayDate) ?? null;"},{"path":"app/app/field-proven-premium-ui.tsx","before":"  if (selectedDay) return <HistoryDayDetail day={selectedDay} onBack={() => setSelectedDayIndex(null)} />;","after":"  if (selectedDay) return <HistoryDayDetail day={selectedDay} phoneTimeZoneLabel={controls.phoneTimeZoneLabel} onBack={() => setSelectedDayDate(null)} />;"},{"path":"app/app/field-proven-premium-ui.tsx","before":"      <div className={styles.screenTopline}><span>ISTORIJA KARTICE</span><small>{state.historyDaysAvailable} od 56 dana</small></div>","after":"      <div className={styles.screenTopline}><span>ISTORIJA KARTICE</span><small>{state.historyDaysAvailable} od 56 dana</small></div>\n      <p>{controls.phoneTimeZoneLabel}</p>"},{"path":"app/app/field-proven-premium-ui.tsx","before":"              onClick={() => setSelectedDayIndex(index)}","after":"              onClick={() => setSelectedDayDate(day.dateLabel)}"},{"path":"app/app/field-proven-premium-ui.tsx","before":"        return <HistoryScreen state={state} />;","after":"        return <HistoryScreen state={state} controls={controls} />;"},{"path":"app/landing-page.tsx","before":"    heroText: \"OLED cockpit za profesionalne vozače. Očitaj karticu preko telefona, vidi poslednjih 56 dana, upozorenja, pauze i korisne informacije bez kopanja po menijima tahografa.\",","after":"    heroText: \"OLED cockpit za profesionalne vozače. Očitaj karticu preko telefona, vidi poslednjih 56 dana, upozorenja, pauze i korisne informacije bez kopanja po menijima tahografa.\",\n    safetyNote: \"Bezbednost pre svega: poveži tahograf i očitavaj karticu samo kada je vozilo bezbedno parkirano. Ne koristi telefon tokom vožnje.\","},{"path":"app/landing-page.tsx","before":"    heroText: \"An OLED cockpit for professional drivers. Read your driver card from your phone, see the last 56 days, warnings, breaks and useful shift information without digging through tachograph menus.\",","after":"    heroText: \"An OLED cockpit for professional drivers. Read your driver card from your phone, see the last 56 days, warnings, breaks and useful shift information without digging through tachograph menus.\",\n    safetyNote: \"Safety first: connect to the tachograph and read the driver card only when the vehicle is safely parked. Do not use your phone while driving.\","},{"path":"app/landing-page.tsx","before":"    heroText: \"OLED-Cockpit für Berufskraftfahrer. Fahrerkarte per Smartphone auslesen, 56 Tage sehen, Warnungen, Pausen und wichtige Schichtinformationen verstehen — ohne Menüsuche im Tachographen.\",","after":"    heroText: \"OLED-Cockpit für Berufskraftfahrer. Fahrerkarte per Smartphone auslesen, 56 Tage sehen, Warnungen, Pausen und wichtige Schichtinformationen verstehen — ohne Menüsuche im Tachographen.\",\n    safetyNote: \"Sicherheit geht vor: Tachograph nur bei sicher geparktem Fahrzeug verbinden und die Fahrerkarte auslesen. Smartphone während der Fahrt nicht benutzen.\","},{"path":"app/landing-page.tsx","before":"          <p>{t.heroText}</p>","after":"          <p>{t.heroText}</p>\n          <p role=\"note\" className=\"tcx-safety-note\"><strong>{t.safetyNote}</strong></p>"},{"path":"app/landing-page.tsx","before":"      <section className=\"tcx-hero\" id=\"top\">","after":"      <section className=\"tcx-hero\" id=\"top\">"},{"path":"tests/v12-card-diagnostics.test.mjs","before":"assert.match(client, /PREVIEW V14/);","after":"assert.match(client, /PREVIEW V15/);"},{"path":"tests/v13-diagnostics-handoff.test.mjs","before":"assert.match(client, /PREVIEW V14/);","after":"assert.match(client, /PREVIEW V15/);"},{"path":"tests/v14-first-card-handoff.test.mjs","before":"assert.match(source, /PREVIEW V14/);","after":"assert.match(source, /PREVIEW V15/);"}];
for (const {path,before,after} of edits) {
  const source=readFileSync(path,"utf8");
  const matches=source.split(before).length-1;
  if (matches!==1) throw new Error("V15 anchor "+path+" matches="+matches+" for "+before.slice(0,110));
  if (before!==after) writeFileSync(path,source.replace(before,after));
}
console.log("V15 timezone projection, phone change detection and parked-only landing applied");
