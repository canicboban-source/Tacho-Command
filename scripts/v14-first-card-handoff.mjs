import {readFileSync,writeFileSync} from "node:fs";
const edits=[{"path":"app/app-v2/app-v2-client.tsx","before":"import { openBrowserAppV2FieldTransport } from \"../../lib/app-v2-field-transport.js\";","after":"import { openBrowserAppV2FieldTransport } from \"../../lib/app-v2-field-transport.js\";\nimport { prepareAppV2CardHandoff } from \"../../lib/app-v2-card-handoff.js\";"},{"path":"app/app-v2/app-v2-client.tsx","before":"    const onUnexpectedDisconnect = () => {\n      if (!cardReadBusyRef.current || lastCardProgressRef.current?.complete) return;\n      const packets = lastCardProgressRef.current?.submessages ?? 0;\n      const kilobytes = ((lastCardProgressRef.current?.byteLength ?? 0) / 1000).toFixed(1);\n      setCardDiagnostic(\n        \"V13 · Chrome je prijavio prekid BLE veze tokom očitavanja: \"\n        + String(packets) + \" paketa, \" + kilobytes + \" KB. Ne ponavljajte čitanje.\"\n      );\n    };\n    selectedDevice?.addEventListener?.(\"gattserverdisconnected\", onUnexpectedDisconnect);","after":"    const onUnexpectedDisconnect = () => {\n      if (!cardReadBusyRef.current || lastCardProgressRef.current?.complete) return;\n      const packets = lastCardProgressRef.current?.submessages ?? 0;\n      const kilobytes = ((lastCardProgressRef.current?.byteLength ?? 0) / 1000).toFixed(1);\n      setCardDiagnostic(packets === 0\n        ? \"V14 · BLE veza se menja pri početku Download sesije; sačekajte da vidimo da li prenos kreće.\"\n        : \"V14 · Chrome je prijavio prekid BLE veze posle \"\n          + String(packets) + \" paketa, \" + kilobytes + \" KB. Ne ponavljajte čitanje.\"\n      );\n    };"},{"path":"app/app-v2/app-v2-client.tsx","before":"      await transport.close({ keepGattConnected: true });\n      // The tachograph needs time to release the Diagnostics service before\n      // the existing GATT link changes to the separate Download service.\n      await new Promise<void>((resolve) => window.setTimeout(resolve, 1200));","after":"      await transport.close({ keepGattConnected: true });\n      // Re-establish only the already selected GATT device if the VU dropped\n      // it while closing Diagnostics. Do not restart the card transfer here.\n      await prepareAppV2CardHandoff({ device: selectedDevice });\n      // Start observing CARD disconnects only after Diagnostics handoff;\n      // an expected handoff disconnect is not a failed card transfer.\n      selectedDevice?.addEventListener?.(\"gattserverdisconnected\", onUnexpectedDisconnect);"},{"path":"app/app-v2/app-v2-client.tsx","before":"    } catch {\n      detachDisconnectListener();\n      setLiveSession(createAppV2LiveSession({\n        phase: \"error\",\n        errorText: \"LIVE veza nije uredno zatvorena pre očitavanja kartice.\",","after":"    } catch (error) {\n      detachDisconnectListener();\n      const handoffReason = error instanceof Error ? error.message : String(error);\n      setCardDiagnostic(\"V14 · Pre čitanja kartice: \" + handoffReason.slice(0, 180));\n      setLiveSession(createAppV2LiveSession({\n        phase: \"error\",\n        errorText: handoffReason.slice(0, 180),"},{"path":"app/app-v2/app-v2-client.tsx","before":"        if (!progress.complete) {\n          setCardDiagnostic((previous) => previous?.includes(\"prekid BLE veze\") ? previous : null);\n        }","after":"        // A fresh valid card packet proves a prior handoff disconnect did not\n        // terminate this transfer; do not preserve a stale fatal-looking label.\n        setCardDiagnostic(null);"},{"path":"app/app-v2/app-v2-client.tsx","before":"    if (result.status === \"accepted\" && result.session?.currentCard) {\n      setCardState(result.session.currentCard);","after":"    if (result.status === \"accepted\" && result.session?.currentCard) {\n      setCardDiagnostic(null);\n      setCardState(result.session.currentCard);"},{"path":"app/app-v2/app-v2-client.tsx","before":"\"V13 · Nema novih paketa 90 s. Ovo ne potvrđuje prekid BLE veze; ne ponavljajte čitanje.\"","after":"\"V14 · Nema novih paketa 90 s. Ovo ne potvrđuje prekid BLE veze; ne ponavljajte čitanje.\""},{"path":"app/app-v2/app-v2-client.tsx","before":"(previous ? previous + \" · \" : \"V13 · \")","after":"(previous ? previous + \" · \" : \"V14 · \")"},{"path":"app/app-v2/app-v2-client.tsx","before":"versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V13\",","after":"versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V14\","},{"path":"lib/app-v2-field-transport.js","before":"      } catch (error) {\n        throw new Error(\"Diagnostics obaveštenja nisu ugašena pre očitavanja kartice: \"\n          + (error instanceof Error ? error.message : String(error)));\n      }\n      if (device.gatt.connected === false) {\n        throw new Error(\"Tahograf je prekinuo BLE pri prelazu LIVE → CARD.\");\n      }\n      return;","after":"      } catch (error) {\n        // The VU may drop GATT after receiving the Diagnostics close. If it did,\n        // all old subscriptions are already invalid; the card handoff preflight\n        // will explicitly reconnect the selected device before any card command.\n        if (device.gatt.connected !== false) {\n          throw new Error(\"Diagnostics obaveštenja nisu ugašena pre očitavanja kartice: \"\n            + (error instanceof Error ? error.message : String(error)));\n        }\n      }\n      return;"},{"path":"tests/v12-card-diagnostics.test.mjs","before":"assert.match(client, /PREVIEW V13/);","after":"assert.match(client, /PREVIEW V14/);"},{"path":"tests/v13-diagnostics-handoff.test.mjs","before":"assert.match(client, /PREVIEW V13/);","after":"assert.match(client, /PREVIEW V14/);"}];
for(const {path,before,after} of edits){
  const content=readFileSync(path,"utf8");
  const count=content.split(before).length-1;
  if(count!==1)throw new Error("V14 patch anchor "+path+" count="+count+" anchor="+before.slice(0,100));
  writeFileSync(path,content.replace(before,after));
}
console.log("V14 first-handoff selected-device GATT preflight and stale-error cleanup applied");
