import { readFileSync, writeFileSync } from "node:fs";
const edits = [{"path":"lib/app-v2-card-transport-controller-bridge.js","before":"  onProgress,\n} = {}) {","after":"  onProgress,\n  onTransportError,\n} = {}) {"},{"path":"lib/app-v2-card-transport-controller-bridge.js","before":"      const transportResult = await readBrowserAppV2GoldenCardPayload({\n        ...transportOptions,\n        onProgress,\n      });\n      return transportResult.payload;","after":"      try {\n        const transportResult = await readBrowserAppV2GoldenCardPayload({\n          ...transportOptions,\n          onProgress,\n        });\n        return transportResult.payload;\n      } catch (error) {\n        try { onTransportError?.(error); } catch {}\n        throw error;\n      }"},{"path":"app/app-v2/app-v2-client.tsx","before":"  close: () => Promise<void>;","after":"  close: (options?: { keepGattConnected?: boolean }) => Promise<void>;"},{"path":"app/app-v2/app-v2-client.tsx","before":"  const [cardReadProgress, setCardReadProgress] = useState<CardReadProgress | null>(null);","after":"  const [cardReadProgress, setCardReadProgress] = useState<CardReadProgress | null>(null);\n  const [cardDiagnostic, setCardDiagnostic] = useState<string | null>(null);"},{"path":"app/app-v2/app-v2-client.tsx","before":"  const cardHandoffRef = useRef(false);","after":"  const cardHandoffRef = useRef(false);\n  const lastCardProgressRef = useRef<CardReadProgress | null>(null);\n  const lastCardProgressAtRef = useRef(0);"},{"path":"app/app-v2/app-v2-client.tsx","before":"    const selectedDevice = transport.device;\n    liveTransportRef.current = null;","after":"    const selectedDevice = transport.device as {\n      gatt?: { connected?: boolean };\n      addEventListener?: (name: string, listener: () => void) => void;\n      removeEventListener?: (name: string, listener: () => void) => void;\n    } | null;\n    setCardDiagnostic(null);\n    lastCardProgressRef.current = null;\n    lastCardProgressAtRef.current = Date.now();\n    const onUnexpectedDisconnect = () => {\n      if (!cardReadBusyRef.current || lastCardProgressRef.current?.complete) return;\n      const packets = lastCardProgressRef.current?.submessages ?? 0;\n      const kilobytes = ((lastCardProgressRef.current?.byteLength ?? 0) / 1000).toFixed(1);\n      setCardDiagnostic(\n        \"V12 · Chrome je prijavio prekid BLE veze tokom očitavanja: \"\n        + String(packets) + \" paketa, \" + kilobytes + \" KB. Ne ponavljajte čitanje.\"\n      );\n    };\n    selectedDevice?.addEventListener?.(\"gattserverdisconnected\", onUnexpectedDisconnect);\n    const detachDisconnectListener = () =>\n      selectedDevice?.removeEventListener?.(\"gattserverdisconnected\", onUnexpectedDisconnect);\n    liveTransportRef.current = null;"},{"path":"app/app-v2/app-v2-client.tsx","before":"    } catch {\n      setLiveSession(createAppV2LiveSession({\n        phase: \"error\",\n        errorText: \"LIVE veza nije uredno zatvorena pre očitavanja kartice.\",","after":"    } catch {\n      detachDisconnectListener();\n      setLiveSession(createAppV2LiveSession({\n        phase: \"error\",\n        errorText: \"LIVE veza nije uredno zatvorena pre očitavanja kartice.\","},{"path":"app/app-v2/app-v2-client.tsx","before":"    setCardReadProgress(Object.freeze({ submessages: 0, byteLength: 0, complete: false }));\n\n    const wakeLock","after":"    setCardReadProgress(Object.freeze({ submessages: 0, byteLength: 0, complete: false }));\n    const stallTimer = window.setInterval(() => {\n      if (!cardReadBusyRef.current || lastCardProgressRef.current?.complete) return;\n      if (Date.now() - lastCardProgressAtRef.current >= 90000) {\n        setCardDiagnostic((previous) => previous ?? (\n          \"V12 · Nema novih paketa 90 s. Ovo ne potvrđuje prekid BLE veze; ne ponavljajte čitanje.\"\n        ));\n      }\n    }, 10000);\n\n    const wakeLock"},{"path":"app/app-v2/app-v2-client.tsx","before":"      onProgress: (progress: CardReadProgress) => setCardReadProgress(progress),","after":"      onProgress: (progress: CardReadProgress) => {\n        lastCardProgressRef.current = progress;\n        lastCardProgressAtRef.current = Date.now();\n        setCardReadProgress(progress);\n        if (!progress.complete) {\n          setCardDiagnostic((previous) => previous?.includes(\"prekid BLE veze\") ? previous : null);\n        }\n      },\n      onTransportError: (error: unknown) => {\n        const reason = error instanceof Error ? error.message : String(error);\n        const packets = lastCardProgressRef.current?.submessages ?? 0;\n        setCardDiagnostic((previous) => (\n          (previous ? previous + \" · \" : \"V12 · \")\n          + \"Greška transporta posle \" + String(packets)\n          + \" paketa: \" + reason.slice(0, 180)\n        ));\n      },"},{"path":"app/app-v2/app-v2-client.tsx","before":"      cardReadBusyRef.current = false;\n      cardHandoffRef.current = false;\n      try { await wakeLock?.release(); } catch {}","after":"      cardReadBusyRef.current = false;\n      cardHandoffRef.current = false;\n      window.clearInterval(stallTimer);\n      detachDisconnectListener();\n      try { await wakeLock?.release(); } catch {}"},{"path":"app/app-v2/app-v2-client.tsx","before":"            cardReadProgress,\n            versionLine: formatTachoCommandVersionLine(),","after":"            cardReadProgress,\n            cardDiagnostic,\n            versionLine: formatTachoCommandVersionLine() + \" · PREVIEW V12\","},{"path":"app/app/field-proven-premium-ui.tsx","before":"  cardReadProgress: Readonly<{\n    submessages: number;\n    byteLength: number;\n    complete: boolean;\n  }> | null;","after":"  cardReadProgress: Readonly<{\n    submessages: number;\n    byteLength: number;\n    complete: boolean;\n  }> | null;\n  cardDiagnostic: string | null;"},{"path":"app/app/field-proven-premium-ui.tsx","before":"          ) : state.cardReadComplete ? <small>{state.historyDaysAvailable}/56 dana sačuvano</small> : null}\n        </div>","after":"          ) : state.cardReadComplete ? <small>{state.historyDaysAvailable}/56 dana sačuvano</small> : null}\n          {controls.cardDiagnostic ? <p role=\"status\">{controls.cardDiagnostic}</p> : null}\n        </div>"}];
for (const {path,before,after} of edits) {
  const source=readFileSync(path,"utf8");
  const matches=source.split(before).length - 1;
  if (matches !== 1) throw new Error("V12 anchor failed: "+path+" "+matches+" "+before.slice(0,90));
  writeFileSync(path,source.replace(before,after));
}
writeFileSync("tests/v12-card-diagnostics.test.mjs", "import assert from \"node:assert/strict\";\nimport test from \"node:test\";\nimport { readFileSync } from \"node:fs\";\ntest(\"V12 reports browser BLE disconnect and packet stall while reading\", () => {\n  const client = readFileSync(\"app/app-v2/app-v2-client.tsx\", \"utf8\");\n  assert.match(client, /gattserverdisconnected/);\n  assert.match(client, /lastCardProgressRef\\.current\\?\\.complete/);\n  assert.match(client, /onTransportError: \\(error: unknown\\)/);\n  assert.match(client, /Nema novih paketa 90 s/);\n  assert.match(client, /cardDiagnostic,/);\n  assert.match(client, /PREVIEW V12/);\n});\ntest(\"V12 keeps golden code untouched and surfaces actual transport failure on card panel\", () => {\n  const bridge = readFileSync(\"lib/app-v2-card-transport-controller-bridge.js\", \"utf8\");\n  assert.match(bridge, /onTransportError\\?\\.\\(error\\)/);\n  assert.match(bridge, /throw error/);\n  const ui = readFileSync(\"app/app/field-proven-premium-ui.tsx\", \"utf8\");\n  assert.match(ui, /role=\"status\">\\{controls\\.cardDiagnostic\\}/);\n});\n");
const wiringPath="tests/app-v2-card-read-ui-wiring.test.mjs";
const wiringSource=readFileSync(wiringPath,"utf8");
const oldAssertion=String.raw`assert.match(source, /onProgress: \(progress: CardReadProgress\) => setCardReadProgress\(progress\)/);`;
const newAssertion=String.raw`assert.match(source, /onProgress: \(progress: CardReadProgress\) => \{/);`;
if (wiringSource.split(oldAssertion).length !== 2) throw new Error("V12 progress test assertion anchor missing");
writeFileSync(wiringPath,wiringSource.replace(oldAssertion,newAssertion));
console.log("V12 passive read-disconnect diagnostics installed.");
